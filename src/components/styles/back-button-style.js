import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  backbutton: {
    alignSelf: 'flex-end',
    height: 30,
    width: 30,
    position: 'absolute',
    top: 42,
    left: 25,
  },
});
