import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  backbutton: {
    alignSelf: 'flex-end',
    height: 50,
    width: 50,
    position: 'absolute',
    top: 42,
    left: 25,
  },
});
